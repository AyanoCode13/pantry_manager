name: meal_planner
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  floor: ^1.5.0
  equatable: ^2.0.8
  sqflite: ^2.4.2
  provider: ^6.1.5+1
  go_router: ^17.1.0
  uuid: ^4.5.3
  logger: ^2.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.1.2
  floor_generator: ^1.4.2

flutter:
  uses-material-design: true
